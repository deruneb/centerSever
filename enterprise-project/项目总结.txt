难点1，不是精灵图里的（单个小图标）做背景时，想要把它调到和它对齐的字体下方时，不管是用背景定位还是给它加宽加高都调不了。
解决方法：用vertical-align:负的数值可以解决。

难点2，用css做二级菜单时，因为给一级菜单的li设置了浮动，所以当二级菜单样式写完后会出现浮动的效果，横着排列。
解决方法：给一级菜单的li设置浮动时用子选择器设置ul下的li浮动。

难点3，二级菜单的隐藏属性是给最外层的二级菜单ul设置，里面的子选项是由a标签撑开。想二级菜单显示时需要给一级菜单设置伪类并指定为二级菜单的ul。

难点4，使用版心，可以解决当浏览器缩小时内容往下排的问题